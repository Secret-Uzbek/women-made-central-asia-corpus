name: Release and Publish

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create release bundle
        run: |
          zip -r women-made-central-asia-corpus-${GITHUB_REF_NAME}.zip . -x ".git/*" ".github/*"
      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: women-made-central-asia-corpus-${{ github.ref_name }}.zip
